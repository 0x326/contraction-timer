import { spacing } from '../../theme/helpers/theme.helpers';
import styled from 'styled-components';

export const StyledLeaderButton = styled.div`
  display: flex;
  justify-content: center;
  margin-bottom: ${spacing('l')};
`;
